/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "firebasestorage.googleapis.com", "img.youtube.com"]
    },
    // reactStrictMode: false
}

module.exports = nextConfig
